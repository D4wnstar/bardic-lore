export type AudioSource = {
    path: string
    recursive: boolean
    active: boolean
}

export type Track = {
    title: string
    album: string
    artist: string
    path: string
    extension: string
}
